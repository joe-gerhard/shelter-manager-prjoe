import styled, { css } from 'styled-components'
import { Theme } from '../../styles/theme'
import { Page } from '../../styles'

type SocialPageProps = {
    theme: Theme;
}

const SocialPage = styled(Page)(({ theme }: SocialPageProps) => css`

`)

export default { 
    SocialPage,
}